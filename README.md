`/web` - https://reviseo.app
`/widget` - IIFE widget build, just puts the iframe logic on a site
`/example` - Example website using the IIFE widget; use pnpm dev:build in `/widget` to move `/dist` to `/example`